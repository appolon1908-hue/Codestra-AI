[project]
name = "codestra-ai"
version = "0.1.0"
description = "Codestra shared AI gateway and policy service"
requires-python = ">=3.12"
dependencies = ["fastapi>=0.116,<1", "uvicorn[standard]>=0.35,<1", "pydantic>=2.11,<3"]
